import React from "react";

import StatusMessage from "./StatusMessage";

const ComponentSpinner = () => (
  <StatusMessage>Downloading components…</StatusMessage>
);

export default ComponentSpinner;
